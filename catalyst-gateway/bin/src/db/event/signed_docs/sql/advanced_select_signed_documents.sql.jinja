SELECT
  signed_docs.id,
  signed_docs.ver,
  signed_docs.type,
  signed_docs.author,
  signed_docs.metadata,
  signed_docs.payload,
  signed_docs.raw
FROM signed_docs
WHERE
  {{ conditions }}
ORDER BY signed_docs.type DESC, signed_docs.id DESC, signed_docs.ver DESC
  {{ query_limits }}
