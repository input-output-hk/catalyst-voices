name: remote_widgets
description: Catalyst remote widgets.
version: 0.1.0+1
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.1"
resolution: workspace

dependencies:
  flutter:
    sdk: flutter
  path: ^1.9.1
  rfw: ^1.0.31

dev_dependencies:
  catalyst_analysis: ^3.0.0

flutter:
  uses-material-design: true
