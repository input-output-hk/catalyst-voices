# For local testing with Docker
VOICES_URL=http://localhost:8080

# Chrome configuration - choose one:
# Option 1: Path to Chrome executable (for local Chrome)
CHROME_EXECUTABLE_PATH="Path to chrome executable"
# Option 2: Chrome DevTools Protocol URL (for remote Chrome)
# CHROME_CDP_URL=ws://localhost:9222/devtools/browser

# Original remote testing configuration (keep for CI/CD)
APP_URL=https://app.dev.projectcatalyst.io/
CHROME_PATH="Path to chrome executable"
CLICK_DELAY=300
ENVIRONMENT=dev