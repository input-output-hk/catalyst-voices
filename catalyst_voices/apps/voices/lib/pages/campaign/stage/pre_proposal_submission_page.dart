import 'package:flutter/widgets.dart';

class PreProposalSubmissionPage extends StatelessWidget {
  const PreProposalSubmissionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
