import 'package:flutter/material.dart';

class ProposalMetadataTile extends StatelessWidget {
  const ProposalMetadataTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      mainAxisSize: MainAxisSize.min,
      children: [
        //
      ],
    );
  }
}
