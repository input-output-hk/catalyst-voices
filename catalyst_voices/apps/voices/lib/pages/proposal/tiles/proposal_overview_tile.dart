import 'package:flutter/material.dart';

class ProposalOverviewTile extends StatelessWidget {
  const ProposalOverviewTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Overview'));
  }
}
