import 'package:flutter/material.dart';

class ProposalCommentsTile extends StatelessWidget {
  const ProposalCommentsTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder(child: Text('Comments'));
  }
}
