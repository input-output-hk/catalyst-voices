/// Utils related to handling web browsers.
final class CatalystBrowser {
  /// Returns `true` if the user is running the application on a Safari browser,
  /// regardless if it's mobile or desktop. `false` otherwise.
  static bool get isSafari {
    throw UnimplementedError('Stub CatalystBrowser');
  }
}
