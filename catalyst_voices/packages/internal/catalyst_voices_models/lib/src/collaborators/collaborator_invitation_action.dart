enum CollaboratorInvitationAction {
  /// The user wishes to accept the invitation to become a collaborator on a proposal.
  accept,

  /// The user wishes to reject the invitation to become a collaborator on a proposal.
  reject,
}
