import 'package:drift/drift.dart';

Future<QueryExecutor> buildExecutor() {
  throw UnsupportedError(
    'buildExecutor() is not implemented on this platform '
    'because neither `dart:ffi` nor `dart:js_interop` are available.',
  );
}
