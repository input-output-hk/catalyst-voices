import 'package:flutter/material.dart';

/// Simple typedef with colors for both background and foreground colors.
typedef SceneColors = ({
  WidgetStateProperty<Color> background,
  WidgetStateProperty<Color> foreground,
});
