name: remote_widgets_example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  catalyst_voices_remote_widgets:
    path: ../
  flutter:
    sdk: flutter
  http: ^1.2.1
  rfw: ^1.0.26

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true