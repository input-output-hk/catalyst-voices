import 'package:flutter/material.dart';

class ProposalBuilderBodyM2 extends StatelessWidget {
  const ProposalBuilderBodyM2({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Proposal Builder'),
      ),
      body: SafeArea(
        child: Center(),
      ),
    );
  }
}
