name: catalyst_compression_example
description: "Demonstrates how to use the catalyst_compression plugin."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  catalyst_compression:
    path: ../
  convert: ^3.1.2
  flutter:
    sdk: flutter

dev_dependencies:
  catalyst_analysis: ^3.0.0
  flutter_test:
    sdk: flutter
  integration_test:
    sdk: flutter

flutter:
  uses-material-design: true
