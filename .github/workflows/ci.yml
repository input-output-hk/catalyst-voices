name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  id-token: write
  contents: write
  packages: write

jobs:
  ci:
    uses: input-output-hk/catalyst-forge/.github/workflows/ci.yml@ci/v1.0.0
    with:
      forge_version: latest