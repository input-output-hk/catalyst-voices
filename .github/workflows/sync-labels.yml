name: Sync labels from catalyst-ci

on:
  workflow_dispatch:
  repository_dispatch:
    types: [catalyst-labels-updated]

permissions:
  issues: write

jobs:
  sync:
    uses: cardano-foundation/catalyst-ci/.github/workflows/sync-labels.yml@master
