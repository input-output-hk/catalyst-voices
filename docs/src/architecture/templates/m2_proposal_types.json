{
  "text": {
    "flutter_widget": "Text widget with style.",
    "properties": {
      "text": "Regex pattern which can be send as JSON string",
      "label_text": "Optional text that describes the input field.",
      "hint_text": "Optional text that provides a hint to the user.",
      "error_text": "Optional text that provides an error message to the user."
    }
  },
  "text_field": {
    "flutter_widget": "TextField widget which reacts on user input",
    "properties": {
      "pattern": "Regex pattern which can be send as JSON string",
      "label_text": "Optional text that describes the input field.",
      "hint_text": "Optional text that provides a hint to the user.",
      "error_text": "Optional text that provides an error message to the user."
    }
  },
  "check_box": {
    "flutter_widget": "M3 CheckBox widget or Catalyst Checkbox widget",
    "properties": {
      "enum": ["category1", "category2", "category3"]
    }
  }
}
